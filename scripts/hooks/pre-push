#!/usr/bin/env bash
# Local pre-push gate. Replaces CI (this repo intentionally runs no GitHub
# Actions). Install with: make install-hooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
echo "pre-push: running make check..."
make check
